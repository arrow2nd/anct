query SearchWorksByKeyword($keyword: String!, $first: Int!) {
  searchWorks(
    titles: [$keyword]
    first: $first
    orderBy: { field: SEASON, direction: ASC }
  ) {
    nodes {
      ...WorkFragment
    }
  }
}
